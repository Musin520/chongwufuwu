spring:
  main:
    allow-circular-references: true
  datasource:
    url: jdbc:h2:mem:acceptance_testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  
# 验收测试专用配置
oss:
  endpoint: http://localhost:9000
  accessKeyId: testkey
  accessKeySecret: testsecret
  bucketName: acceptance-test-bucket

logging:
  level:
    com.example: DEBUG
    org.springframework.web: DEBUG